/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    // experimental: {
    //     appDir: true, // testing app directory
    //   },
}

module.exports = nextConfig
